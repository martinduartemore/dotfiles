# zsh prompt
# set terminal title (xterm-compatible)
case "$TERM" in
  xterm*|rxvt*|eterm*|screen*|tmux*) print -Pn "\e]0;%n@%m: %~\a" ;;
esac

# prompt: green user@host, blue cwd, newline then prompt char
PROMPT='%F{green}%n@%m%f:%F{blue}%~%f'
PROMPT+=$'\n'
PROMPT+=$'λ '
